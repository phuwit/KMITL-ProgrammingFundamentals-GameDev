ed
    // SoundBuffer deathBuffer;
    // deathBuffer.loadFromFile("sound/death.wav");
    // Sound death;
    // death.setBuffer(deathBuffer);
    // // out of time
    // SoundBuffer ootBuffer;
    // ootBuffer.loadFromFile("sound/out_of_time.wav");
    // Sound outOfTime;
    // outOfTime.setBuffer(ootB